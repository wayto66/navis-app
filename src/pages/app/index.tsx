/* eslint-disable react-hooks/exhaustive-deps */
import { type NextPage } from "next";

const AppHomePage: NextPage = () => {
  return (
    <main className="flex min-h-screen w-full items-start justify-center"></main>
  );
};

export default AppHomePage;
